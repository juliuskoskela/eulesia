keys:
  # julius.koskela@digimuoto.com workstation
  - &local_dev age1xy93kan6jxmp8wwayarejuvkxh99cfjyxdfwdryv0axr8dds5dhqws809e
  # local MicroVM sops key used by just vm-run / just vm-deploy
  - &local_vm age1r0y06ntxaku4ugt536mpz6e8tz08jkq6w84uxl4yhstgmmejlsxqdy9knp
  # eulesia-server-test
  - &test_host age1up3c8m6l4dkzerpa5dgnd5y2tq24lprdc60xj3unv6dg3jp6c5zqwf0jv0

creation_rules:
  # Local and test deployment secrets.
  - path_regex: secrets/test/[^/]+\.enc$
    age:
      - *local_dev
      - *local_vm
      - *test_host

  # Production deployment secrets.
  - path_regex: secrets/prod/[^/]+\.enc$
    age:
      - *local_dev
      # Add the production host age recipient here after bootstrapping it.
